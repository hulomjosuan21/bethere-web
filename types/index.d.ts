export enum USER_ENTITY_ENUM {
  STUDENT_USER = "student_user",
  DEPARTMENT_USER = "department_user",
  SCHOOL_USER = "school_user",
}

export enum USER_ROLE_ENUM {
  STUDENT = "student",
  STAFF = "staff",
  ADMIN = "admin",
}
